<div class="menu-container">
  <div class="card-header col-md-10">
    <h2>
      &#127869 Current Menu
    </h2>
  </div>
  <!-- We implement the built-in helper, #each. This helper will iterate over dishes 
  This dishes is the one in the dish0routes.js so the whole dishes array and the dish is one of the dishes it will loop throught each one-->
  {{#each dishes as |dish|}}<!--This will cycle through all the dishes and repeat the code for all the dishes-->
  <!-- For each dish in the array of dish objects, the following <div> will be rendered. -->
    <div class="dish col-md-5">
      <!-- We use dish.id instead of <od> and <li> elements. -->
      <p>
        {{dish.id}}.<!--being delivered to us from the server-->
        {{dish.dish_name}}
      </p>
    </div>
    <div class="col-md-7">
      <p>
        <!-- guest_name will eventually go here -->
      </p>
    </div>
    <!-- Below, we close the #each helper. -->
  {{/each}}<!--To end the built in helper-->
</div>
<!--This helper will make it less lines of code and less repeating of code-->
<!--The start is the # and the end is the /-->